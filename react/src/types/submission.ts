export enum SubmissionStatus {
    Active = 'Active',
    Inactive = 'Inactive',
    Pending = 'Pending'
}

export enum SubmissionAction {
    Approve = 'Approve',
    Reject = 'Reject',
    Waiting = 'Waiting'
}

export enum UserSubmissionAction {
    Cancel = 'Canceled',
    Request = 'Requested'
}
